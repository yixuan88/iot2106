1. scp the whole IoTProject folder into the rpi
2. ssh into the rpi and cd IoTProject
3. cd to /setup
4. inside the setup folder run sudo bash autosetup.sh

the webpage is always:
IP to put in browser: http://192.168.4.1:5000
Wifi SSID: MeshGateway-<Pi_HostName>
Password: password