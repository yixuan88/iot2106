# Standard MQTT — Python bridge connects here (localhost only)
listener 1883 127.0.0.1
allow_anonymous true

# WebSocket — browsers connect here via MQTT.js (bound to AP interface only)
listener 9001 192.168.4.1
protocol websockets
allow_anonymous true
